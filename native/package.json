{
  "name": "claude-parser",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "claude-parser"
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "npx @napi-rs/cli build --platform --release",
    "build:debug": "npx @napi-rs/cli build --platform",
    "prepublishOnly": "napi prepublish -t npm",
    "test": "cargo test",
    "version": "napi version"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  }
}
